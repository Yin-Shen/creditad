# Exact lockfile for CrediTAD: the versions actually imported by the interpreter the test
# suite runs under (CPython 3.13), re-read from importlib.metadata on
# 2026-07-25 (issue F6). Python 3.13; the project targets >=3.10.
# For a flexible floor-range install use requirements.txt; for exact reproduction use this file:
#   pip install -r requirements.lock.txt
#
# Corrected 2026-07-25: this file previously pinned pandas==3.0.2, which is NOT the tested
# version — the environment these tests pass in runs pandas 2.3.1. Analysis-only pins
# (matplotlib / pyliftover / python-docx / torch) moved to requirements-analyses.txt;
# pyfaidx / seaborn / xgboost dropped (installed in the dev image but imported nowhere).
# Linux/macOS snapshot: winbbi (the Windows bigWig reader) is intentionally absent here --
# it is declared with a platform marker in requirements.txt and installs only on Windows.
aiohttp-cors==0.8.1
aiohttp==3.13.4
cooler==0.10.4
cooltools==0.7.1
h5py==3.16.0
hic-straw==1.3.1
hmmlearn==0.3.3
joblib==1.5.3
numpy==2.4.4
pandas==2.3.1
pyBigWig==0.3.25
pytest==9.0.3
scikit-image==0.26.0
scikit-learn==1.8.0
scipy==1.17.1
sqlalchemy==2.0.48
tqdm==4.67.3
